overwrite: true
schema: "./schema.graphql"  # point this to your actual GraphQL endpoint
generates:
  lib/gql/types.ts:  # The output file
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"

